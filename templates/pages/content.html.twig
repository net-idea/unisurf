{% extends 'base.html.twig' %}

{% block title %}
  {{ pageMeta.title|default('UniSurf') }}
{% endblock %}

{% block body %}
  <main class="flex-shrink-0">
    <div class="container">
      {{ content|raw }}
    </div>
  </main>
{% endblock %}
